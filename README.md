# TEST REACT 

## Build Setup

```bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run start

# Также в самой верхней ячейке каждого столбца есть сортировка, которая появляется при щелчке на ячейку и осуществляет сортировку в выбранном столбце.
